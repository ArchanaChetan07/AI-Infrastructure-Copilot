[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*

markers =
    integration: requires ANTHROPIC_API_KEY — makes real LLM API calls
    slow: runs all 3 scenarios end-to-end (~60s, ~$0.08)
